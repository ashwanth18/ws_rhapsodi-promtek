global:
  resolve_timeout: 5m

route:
  receiver: default
  group_by: [alertname, instance]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  # Placeholder receiver (no-op). Add slack_configs / email_configs when ready.
  - name: default
